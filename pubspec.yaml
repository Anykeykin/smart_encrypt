name: smart_ecrypt
description: library for easy use encrypt & decrypt features, generate bytes & string
version: 1.0.0

environment:
  sdk: ^3.1.5

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
